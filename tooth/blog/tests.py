from django.test import TestCase
from django.utils import timezone
from . import models
import random
import time

class PostTest(TestCase):
	pass
	# post = Post()

	# post.title = 'random test nr' + str(time.time())
	# 